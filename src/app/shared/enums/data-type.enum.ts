export enum DataTypeEnum {
    STRING = 'string',
    TEXTAREA = 'textarea',
    PASSWORD = 'password',
    NUMBER = 'number',
    DATETIME = 'datetime',
    DATE = 'date',
    SELECT_SINGLE = 'selectSingle',
    SELECT_MULTIPLE = 'selectMultiple',
}
